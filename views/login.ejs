<!DOCTYPE html>
<html lang="<%= lang %>">
<head>
  <%- include('includes/head') %>
  <link rel="stylesheet" href="/style.css">
</head>
<body>

<h3 style="text-align:center; margin-top:20px;">Welcome to eDreamBook</h3>

<main class="auth-wrapper">

  <div class="left-side">
    <div class="landing-image">

      <form action="/login" method="POST" class="auth-form">

        <% if (Array.isArray(errors) && errors.length > 0) { %>
          <% errors.forEach(function(err) { %>
            <p class="auth-error"><%= err %></p>
          <% }) %>
        <% } %>

        <fieldset class="login-form">
          <legend>Login</legend>

          <label class="input" for="username">username</label>
          <input type="text" id="username" name="username" autocomplete="off" required>
<br>
          <label class="input" for="password">password</label>
          <input type="password" id="password" name="password" autocomplete="off" required>
<br>
          <button class="btn" type="submit">Login</button>

          <p class="auth-links">
            Don't have an account? <a href="/register">Sign up</a>
          </p>

          <p class="auth-links">
            Admin? <a href="/admin-login">Login here</a>
          </p>

          <!-- Password reset link -->
          <p class="auth-links">
            <a href="/password-reset">Forgot your password?</a>
          </p>

        </fieldset>
      </form>
    </div>
  </div>

  <div class="right-side">
    <div class="landing-content">

      <p class="subtitle">
        Discover the meaning behind your dreams and explore our rich library of dream interpretations.
      </p>

      <ul style="color: blueviolet; font-size: 18px;" class="features">
        <li> Search thousands of dream meanings</li>
        <li> Learn spiritual and psychological interpretations</li>
        <li> Save your dream history</li>
        <li> Access dream dictionary anytime</li>
      </ul>

      <div class="illustration-box">
        <p>Your dreams have meaning — unlock them with DreamBook.</p>
      </div>
    </div>
  </div>

</main>
</body>
</html>

<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('includes/head') %>
    <link rel="stylesheet" href="/style.css">
</head>

<body>
<%- include('includes/header', { user, notifications: notifications || [] }) %>

<main class="auth-two-column">

    <!-- LEFT: LOGIN FORM -->
    <div class="left-column">
        <form action="/login" method="POST" class="auth-form">

            <% if (errors && errors.length) { %>
                <% errors.forEach(function(error) { %>
                    <p class="auth-error"><%= error %></p>
                <% }) %>
            <% } %>

            <fieldset class="login-form">
                <legend>Login</legend>

                <label for="username"><small>Username</small></label>
                <input type="text" id="username" name="username" required>

                <label for="password"><small>Password</small></label>
                <input type="password" id="password" name="password" required>

                <button type="submit">Login</button>

                <p class="auth-links">Don't have an account? <a href="/register">Sign up</a></p>
                <p class="auth-links">Admin? <a href="/admin-login">Login here</a></p>
            </fieldset>
        </form>
    </div>

    <!-- RIGHT: LANDING CONTENT -->
    <div class="right-column">
        <div class="landing-content">
            <h1>Welcome to <span class="brand">DreamBook</span></h1>

            <p class="subtitle">
                Discover the meaning behind your dreams and explore our rich library of dream interpretations.
            </p>

            <ul class="features">
                <li>✓ Search thousands of dream meanings</li>
                <li>✓ Learn spiritual and psychological interpretations</li>
                <li>✓ Save your dream history</li>
                <li>✓ Access dream dictionary anytime</li>
            </ul>

            <div class="illustration-box">
                <p>Your dreams have meaning — unlock them with DreamBook.</p>
            </div>
        </div>
    </div>

</main>

</body>
</html>


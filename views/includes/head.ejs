<%
  const pageTitle =
    typeof title !== "undefined"
      ? title
      : "DreamBook – Dreams Interpretation, Analyzer & Dictionary";

  const pageDescription =
    typeof description !== "undefined"
      ? description
      : "Explore dream meanings, dream analyzer, and dream interpretation on DreamBook.";
%>

<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1">

<title><%= pageTitle %></title>
<meta name="description" content="<%= pageDescription %>">

<link rel="canonical" href="https://dreambook.com.et<%= typeof canonical !== 'undefined' ? canonical : '' %>">

<link rel="stylesheet" href="/style.css">
<link rel="icon" href="/favicon.ico">

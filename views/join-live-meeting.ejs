<!DOCTYPE html>
<html lang="<%= lang %>">
<head>
  <%- include('includes/head') %>
  <link rel="stylesheet" href="/style.css">
</head>
<body>
<%- include('includes/header') %>

<div class="container mt-4">
  <div class="d-flex justify-content-between align-items-center mb-3">
    <h1>Live Meetings</h1>
    <form action="/live-meetings/create" method="post">
      <button type="submit" class="btn btn-primary">Create New Meeting</button>
    </form>
  </div>
  <% if(meetings && meetings.length){ %>
    <ul class="list-group">
      <% meetings.forEach(m => { %>
        <li class="list-group-item d-flex justify-content-between align-items-center">
          <div>
            <strong><%= m.title || 'Untitled' %></strong>
            <% if(m.scheduled_at){ %> – <%= new Date(m.scheduled_at).toLocaleString() %> <% } %>
            <% if(m.status){ %> [<%= m.status %>] <% } %>
          </div>
          <form action="/live-meetings/<%= m.id %>" method="post">
            <button type="submit" class="btn btn-success btn-sm">Join via Link</button>
          </form>
        </li>
      <% }) %>
    </ul>
  <% } else { %>
    <p>No active meetings. You can create one <a href="/live-meetings/create">here</a>.</p>
  <% } %>
</div>
</body>
</html>